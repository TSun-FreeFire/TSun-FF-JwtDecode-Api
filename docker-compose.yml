version: "3.8"

services:
  tsun-ff-jwtdecode-api:
    build: .
    container_name: tsun-ff-jwtdecode-api
    restart: always
    ports:
      - "127.0.0.1:8078:8078"
    environment:
      - PYTHONUNBUFFERED=1
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8078/')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
